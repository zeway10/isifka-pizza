<!DOCTYPE html>
<html lang="ku" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>Isifka Pizza</title>
<!-- Tailwind CSS -->
<script src="https://cdn.tailwindcss.com"></script>
<!-- Google Fonts & Icons -->
<link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@300;400;600;700;800&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<!-- QR Code Library -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
<style>
body {
font-family: 'Noto Kufi Arabic', sans-serif;
background-color: #f4f4f9;
-webkit-tap-highlight-color: transparent;
scroll-behavior: smooth;
}
.hide-scrollbar::-webkit-scrollbar { display: none; }
.hide-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
.glass-header {
background: rgba(255, 255, 255, 0.85);
backdrop-filter: blur(12px);
-webkit-backdrop-filter: blur(12px);
border-bottom: 1px solid rgba(255, 255, 255, 0.3);
}
.text-gradient {
background: linear-gradient(135deg, #f43f5e 0%, #fb923c 100%);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
}
.cat-btn {
background: white;
border: 1px solid #e2e8f0;
color: #64748b;
transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
.cat-btn.active {
background: linear-gradient(135deg, #f43f5e 0%, #fb923c 100%);
color: white;
border-color: transparent;
box-shadow: 0 4px 15px -3px rgba(244, 63, 94, 0.4);
transform: translateY(-2px);
}
.food-card {
transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.food-card:hover {
transform: translateY(-5px);
box-shadow: 0 15px 30px -5px rgba(0, 0, 0, 0.1);
}
.modal-enter { animation: slideUp 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards; }
@keyframes slideUp {
from { transform: translateY(100%); opacity: 0; }
to { transform: translateY(0); opacity: 1; }
}
.fade-in { animation: fadeIn 0.3s ease-out forwards; }
@keyframes fadeIn {
from { opacity: 0; backdrop-filter: blur(0px); }
to { opacity: 1; backdrop-filter: blur(5px); }
}
.hero-bg {
background-image: url('https://images.unsplash.com/photo-1504674900247-0877df9cc836?q=80&w=1000&auto=format&fit=crop');
background-position: center;
background-size: cover;
}
</style>
</head>
<body class="text-slate-800 pb-28 relative min-h-screen">
<!-- Header -->
<header class="glass-header fixed top-0 w-full z-40 transition-all duration-300 shadow-sm">
<div class="container mx-auto px-4 h-16 flex justify-between items-center max-w-4xl">
<div class="flex items-center gap-3">
<div class="w-10 h-10 bg-gradient-to-tr from-rose-500 to-orange-400 rounded-2xl flex items-center justify-center text-white text-xl shadow-lg shadow-rose-200 rotate-3 relative">
<i class="fa-solid fa-utensils -rotate-3"></i>
<span id="sync-status" class="absolute -top-1 -right-1 w-3 h-3 bg-green-500 border-2 border-white rounded-full hidden" title="Connected to Backend"></span>
</div>
<div>
<h1 class="font-black text-xl leading-tight text-gradient tracking-widest" id="ui-brand-title">Isifka Pizza</h1>
</div>
</div>
<div class="flex items-center gap-2">
<button onclick="promptAdmin()" class="w-10 h-10 rounded-full bg-slate-100 text-slate-500 flex items-center justify-center hover:bg-slate-200 transition-all shadow-inner" title="بەشی بەڕێوەبەر">
<i class="fa-solid fa-lock text-sm"></i>
</button>
<button onclick="toggleLanguage()" class="w-10 h-10 rounded-full bg-slate-100 text-slate-700 font-bold text-sm flex items-center justify-center hover:bg-slate-200 transition-all shadow-inner border border-slate-200" id="ui-btn-lang">
ع
</button>
<button onclick="toggleCart()" class="relative w-10 h-10 flex items-center justify-center text-rose-500 bg-rose-50 rounded-full hover:bg-rose-100 transition-colors">
<i class="fa-solid fa-basket-shopping text-xl"></i>
<span id="cart-badge" class="absolute -top-1 -right-1 bg-rose-500 text-white text-[11px] font-bold w-5 h-5 rounded-full flex items-center justify-center hidden shadow-md border-2 border-white">0</span>
</button>
</div>
</div>
<div class="container mx-auto max-w-4xl">
<div id="categories-container" class="flex overflow-x-auto hide-scrollbar gap-2 px-4 py-3 border-t border-slate-100/50"></div>
</div>
</header>
<main class="container mx-auto px-4 max-w-4xl mt-36">
<div class="w-full h-40 sm:h-56 rounded-3xl overflow-hidden mb-8 shadow-xl relative hero-bg">
<div class="absolute inset-0 bg-gradient-to-t from-slate-900/90 via-slate-900/40 to-transparent flex flex-col justify-end p-6">
<h2 class="text-white text-2xl sm:text-3xl font-black mb-1 drop-shadow-md" id="ui-hero-title">تامەکا ژبیرنەکری</h2>
<p class="text-slate-200 text-sm sm:text-base font-light flex items-center gap-2">
<i class="fa-solid fa-star text-orange-400 text-xs"></i> <span id="ui-hero-subtitle">تامکرنا باشترین خوارنان</span>
</p>
</div>
</div>
<div id="loading-indicator" class="flex flex-col items-center justify-center py-12 text-slate-400">
<i class="fa-solid fa-circle-notch fa-spin text-3xl mb-3 text-blue-500"></i>
<p class="font-bold">بەستنەوە بە داتابەیس...</p>
</div>
<div id="menu-items-container" class="grid grid-cols-1 sm:grid-cols-2 gap-5 hidden"></div>
</main>
<!-- Cart Modal -->
<div id="cart-modal" class="fixed inset-0 z-50 hidden">
<div class="absolute inset-0 bg-slate-900/40 backdrop-blur-sm fade-in" onclick="toggleCart()"></div>
<div class="absolute bottom-0 left-0 right-0 bg-white rounded-t-[30px] shadow-2xl max-h-[90vh] flex flex-col md:max-w-md md:mx-auto md:top-10 md:bottom-auto md:rounded-[30px] transform transition-transform" id="cart-content">
<div class="p-5 border-b border-slate-100 flex justify-between items-center bg-white rounded-t-[30px]">
<h2 class="text-xl font-black flex items-center gap-2 text-slate-800" id="ui-cart-title">
<i class="fa-solid fa-basket-shopping text-rose-500"></i> سەلکا من
</h2>
<div class="flex items-center gap-2">
<button onclick="clearCart()" id="ui-btn-clear-cart" class="hidden px-3 py-1.5 bg-red-50 text-red-500 rounded-xl text-xs font-bold hover:bg-red-100 transition-colors shadow-sm border border-red-100 flex items-center gap-1.5">
<i class="fa-solid fa-trash-can"></i> <span>پاشگەزبوونەوە</span>
</button>
<button onclick="toggleCart()" class="w-9 h-9 rounded-full bg-slate-100 flex items-center justify-center text-slate-500 hover:bg-slate-200 transition-colors">
<i class="fa-solid fa-times text-lg"></i>
</button>
</div>
</div>
<div id="cart-items-container" class="p-5 overflow-y-auto flex-1 flex flex-col gap-3 bg-slate-50">
<div class="text-center text-slate-400 py-12 flex flex-col items-center justify-center h-full" id="empty-cart-msg">
<div class="w-24 h-24 bg-slate-100 rounded-full flex items-center justify-center mb-4">
<i class="fa-solid fa-basket-shopping text-4xl opacity-40"></i>
</div>
<p id="ui-empty-cart" class="font-medium">سەلک ڤالایە</p>
</div>
</div>
<div class="p-5 bg-white border-t border-slate-100 rounded-b-[30px] shadow-[0_-10px_15px_-3px_rgba(0,0,0,0.05)]">
<div class="flex gap-3 mb-4">
<div class="w-1/3">
<label id="ui-table-label" for="table-number" class="block text-xs font-bold text-slate-500 mb-1.5">ژمارەی مێز:</label>
<input type="number" id="table-number" min="1" class="w-full bg-slate-50 border border-slate-200 rounded-xl p-3 text-sm text-center focus:border-rose-500 focus:ring-1 focus:ring-rose-200 outline-none transition-all font-black text-slate-700" placeholder="0">
</div>
<div class="w-2/3">
<label id="ui-notes-label" for="order-notes" class="block text-xs font-bold text-slate-500 mb-1.5">تێبینی:</label>
<input type="text" id="order-notes" class="w-full bg-slate-50 border border-slate-200 rounded-xl p-3 text-sm focus:border-rose-500 focus:ring-1 focus:ring-rose-200 outline-none transition-all" placeholder="بۆ نموونە: بێ پیاز...">
</div>
</div>
<div class="flex justify-between items-center mb-5 font-bold text-lg">
<span id="ui-total-text" class="text-slate-600">کۆژمێ گشتی:</span>
<span id="cart-total" class="text-2xl text-slate-800 font-black tracking-tight" dir="ltr">0 IQD</span>
</div>
<button onclick="checkout()" class="w-full bg-gradient-to-r from-rose-500 to-orange-500 hover:from-rose-600 hover:to-orange-600 text-white font-bold py-4 rounded-2xl shadow-lg shadow-rose-200 transition-transform active:scale-95 flex items-center justify-center gap-3 text-lg" id="ui-btn-checkout">
هنارتنا داخازیێ <i class="fa-brands fa-whatsapp text-2xl"></i>
</button>
</div>
</div>
</div>
<!-- Auth Modal -->
<div id="auth-modal" class="fixed inset-0 z-[70] hidden flex items-center justify-center">
<div class="absolute inset-0 bg-slate-900/60 backdrop-blur-md fade-in" onclick="closeAuthModal()"></div>
<div class="bg-white p-6 rounded-3xl shadow-2xl z-10 w-[90%] max-w-sm relative modal-enter">
<button onclick="closeAuthModal()" class="absolute top-4 left-4 w-8 h-8 rounded-full bg-slate-100 text-slate-500 flex items-center justify-center">
<i class="fa-solid fa-times"></i>
</button>
<div class="text-center mb-6 mt-4">
<div class="w-16 h-16 bg-blue-100 text-blue-500 rounded-2xl flex items-center justify-center mx-auto mb-3 rotate-3 shadow-inner">
<i class="fa-solid fa-shield-halved text-2xl -rotate-3"></i>
</div>
<h3 class="font-bold text-xl text-slate-800" id="ui-auth-title">چوونەژوورەوەی بەڕێوەبەر</h3>
<p class="text-xs text-slate-500 mt-1" id="ui-auth-desc">تکایە تێپەڕەوشە بنووسە</p>
</div>
<form id="auth-form" onsubmit="handleAuth(event)">
<input type="password" id="admin-pass-input" class="w-full bg-slate-50 border border-slate-200 rounded-xl p-3 text-center text-lg tracking-[0.3em] font-bold focus:border-blue-500 focus:ring-2 focus:ring-blue-200 outline-none transition-all mb-4" placeholder="•••••" dir="ltr" required>
<button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-xl transition-colors shadow-lg shadow-blue-200" id="ui-btn-login">چوونەژوورەوە</button>
</form>
</div>
</div>
<!-- Admin Panel Modal -->
<div id="admin-modal" class="fixed inset-0 z-[60] hidden">
<div class="absolute inset-0 bg-slate-900/80 backdrop-blur-md fade-in"></div>
<div class="absolute inset-0 overflow-y-auto py-6 px-4 flex justify-center">
<div class="bg-slate-50 w-full max-w-3xl rounded-[30px] shadow-2xl relative flex flex-col my-auto min-h-[90vh]" id="admin-content">
<div class="p-5 border-b border-slate-200 flex justify-between items-center bg-white rounded-t-[30px] sticky top-0 z-20 shadow-sm">
<div class="flex items-center gap-3">
<div class="w-10 h-10 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center">
<i class="fa-solid fa-sliders"></i>
</div>
<h2 class="text-xl font-bold text-slate-800" id="ui-admin-title">ڕێڤەبەری / الإدارة</h2>
</div>
<div class="flex items-center gap-2">
<button onclick="logoutAdmin()" class="px-4 h-9 rounded-full bg-red-50 flex items-center justify-center text-red-600 hover:bg-red-100 transition-colors gap-2 text-sm font-bold shadow-inner">
<i class="fa-solid fa-lock"></i> <span id="ui-btn-lock">قوفڵکردن</span>
</button>
<button onclick="closeAdminPanel()" class="w-9 h-9 rounded-full bg-slate-100 flex items-center justify-center text-slate-500 hover:bg-slate-200 transition-colors">
<i class="fa-solid fa-times"></i>
</button>
</div>
</div>
<div class="p-5 flex-1 overflow-y-auto">
<div class="flex flex-wrap gap-3 justify-between items-center mb-6 bg-white p-4 rounded-2xl shadow-sm border border-slate-100">
<h3 class="font-bold text-lg text-slate-700" id="ui-admin-items-title"><i class="fa-solid fa-list-ul ml-2 text-blue-400"></i> لیستا خوارنان</h3>
<div class="flex gap-2">
<button onclick="showQRCode()" class="bg-slate-100 text-slate-700 px-4 py-2.5 rounded-xl text-sm font-bold shadow-sm hover:bg-slate-200 hover:-translate-y-0.5 transition-all flex items-center gap-2">
<i class="fa-solid fa-qrcode"></i> <span class="hidden sm:inline">QR Code</span>
</button>
<button onclick="openEditForm(null)" class="bg-blue-600 text-white px-5 py-2.5 rounded-xl text-sm font-bold shadow-lg shadow-blue-200 hover:bg-blue-700 hover:-translate-y-0.5 transition-all flex items-center gap-2">
<i class="fa-solid fa-plus"></i> <span id="ui-btn-add-new">زێدەکرن</span>
</button>
</div>
</div>
<div id="admin-items-list" class="grid grid-cols-1 sm:grid-cols-2 gap-4"></div>
<div id="admin-form-container" class="hidden bg-white p-6 rounded-3xl border border-slate-200 shadow-xl mt-6 relative overflow-hidden">
<div class="absolute top-0 right-0 w-32 h-32 bg-blue-50 rounded-bl-full -z-10"></div>
<h4 class="font-black text-xl text-slate-800 mb-6 flex items-center gap-2" id="ui-form-title">
<i class="fa-solid fa-pen-to-square text-blue-500"></i> گوهۆرین
</h4>
<form id="admin-form" class="flex flex-col gap-5">
<input type="hidden" id="form-id">
<div class="grid grid-cols-1 md:grid-cols-2 gap-5">
<div class="flex flex-col gap-3 bg-slate-50/50 p-4 rounded-2xl border border-slate-100">
<h5 class="text-sm font-bold text-slate-700 flex items-center gap-2 mb-1">
<span class="w-2 h-2 rounded-full bg-green-500"></span> کوردی (بادینی)
</h5>
<div>
<label class="block text-xs font-semibold text-slate-500 mb-1">ناڤی خوارنێ</label>
<input type="text" id="form-name-ku" required class="w-full bg-white border border-slate-200 rounded-xl p-2.5 text-sm focus:border-blue-500 focus:ring-1 outline-none">
</div>
<div>
<label class="block text-xs font-semibold text-slate-500 mb-1">پێزانین</label>
<textarea id="form-desc-ku" class="w-full bg-white border border-slate-200 rounded-xl p-2.5 text-sm h-20 focus:border-blue-500 focus:ring-1 outline-none resize-none"></textarea>
</div>
</div>
<div class="flex flex-col gap-3 bg-slate-50/50 p-4 rounded-2xl border border-slate-100" dir="rtl">
<h5 class="text-sm font-bold text-slate-700 flex items-center gap-2 mb-1">
<span class="w-2 h-2 rounded-full bg-amber-500"></span> عەرەبی (العربية)
</h5>
<div>
<label class="block text-xs font-semibold text-slate-500 mb-1">اسم الطبق</label>
<input type="text" id="form-name-ar" required class="w-full bg-white border border-slate-200 rounded-xl p-2.5 text-sm focus:border-blue-500 focus:ring-1 outline-none">
</div>
<div>
<label class="block text-xs font-semibold text-slate-500 mb-1">الوصف</label>
<textarea id="form-desc-ar" class="w-full bg-white border border-slate-200 rounded-xl p-2.5 text-sm h-20 focus:border-blue-500 focus:ring-1 outline-none resize-none"></textarea>
</div>
</div>
</div>
<div class="grid grid-cols-1 sm:grid-cols-2 gap-5 mt-2 bg-slate-50/50 p-4 rounded-2xl border border-slate-100">
<div>
<label class="block text-xs font-semibold text-slate-500 mb-1">بها / السعر (د.ع)</label>
<div class="relative">
<input type="number" id="form-price" min="0" step="250" required class="w-full bg-white border border-slate-200 rounded-xl p-2.5 text-sm font-bold text-left focus:border-blue-500 focus:ring-1 outline-none" dir="ltr" placeholder="0">
<span class="absolute left-3 top-2.5 text-slate-400 font-bold text-sm">IQD</span>
</div>
</div>
<div>
<label class="block text-xs font-semibold text-slate-500 mb-1">جۆر / الصنف</label>
<select id="form-category" class="w-full bg-white border border-slate-200 rounded-xl p-2.5 text-sm font-medium focus:border-blue-500 focus:ring-1 outline-none"></select>
</div>
<div class="col-span-1 sm:col-span-2 border-t border-slate-200 pt-4 mt-2">
<label class="block text-xs font-semibold text-slate-500 mb-2">وێنەیەک باربکە / رفع صورة</label>
<div class="flex items-center gap-4">
<label class="flex-1 cursor-pointer bg-white border-2 border-dashed border-slate-300 hover:border-blue-500 rounded-xl p-4 text-center transition-colors">
<i class="fa-solid fa-cloud-arrow-up text-2xl text-slate-400 mb-2"></i>
<span class="block text-xs text-slate-500 font-bold">کرتە بکە بۆ هەڵبژاردنی وێنە</span>
<input type="file" id="form-image-file" accept="image/*" class="hidden" onchange="handleImageUpload(event)">
</label>
<div id="image-preview-container" class="hidden w-24 h-24 rounded-xl border border-slate-200 overflow-hidden relative shadow-sm shrink-0 bg-slate-100">
<img id="image-preview" src="" class="w-full h-full object-cover">
<button type="button" onclick="removeUploadedImage()" class="absolute top-1 right-1 w-6 h-6 bg-slate-900/60 hover:bg-red-500 text-white rounded-full flex items-center justify-center text-xs shadow-md transition-colors">
<i class="fa-solid fa-times"></i>
</button>
</div>
</div>
</div>
</div>
<div class="flex gap-3 mt-4 pt-4 border-t border-slate-100">
<button type="submit" class="flex-1 bg-green-500 hover:bg-green-600 text-white py-3 rounded-xl shadow-lg shadow-green-200 font-bold transition-transform active:scale-95 flex items-center justify-center gap-2" id="ui-btn-save">پاراستن / حفظ</button>
<button type="button" onclick="closeEditForm()" class="flex-1 bg-slate-200 hover:bg-slate-300 text-slate-700 py-3 rounded-xl font-bold transition-colors" id="ui-btn-cancel">هەلوەشاندن / إلغاء</button>
</div>
</form>
</div>
</div>
<div class="p-4 bg-red-50/50 rounded-b-[30px] text-center border-t border-red-100">
<button onclick="attemptReset()" class="text-xs font-bold text-red-500 hover:text-red-700 transition-colors bg-white px-4 py-2 rounded-full shadow-sm border border-red-100">
<i class="fa-solid fa-triangle-exclamation"></i> گەڕاندنەوە بۆ داتای سەرەتایی / إعادة ضبط
</button>
</div>
</div>
</div>
</div>
<!-- QR Code Modal -->
<div id="qr-modal" class="fixed inset-0 z-[85] hidden flex items-center justify-center">
<div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm fade-in" onclick="closeQRCode()"></div>
<div class="bg-white p-6 rounded-3xl shadow-2xl z-10 w-[90%] max-w-sm text-center modal-enter relative">
<button onclick="closeQRCode()" class="absolute top-4 left-4 w-8 h-8 rounded-full bg-slate-100 text-slate-500 flex items-center justify-center hover:bg-slate-200 transition-colors">
<i class="fa-solid fa-times"></i>
</button>
<h3 class="font-bold text-xl text-slate-800 mb-2 mt-2">QR Code</h3>
<p class="text-sm text-slate-500 mb-6">ئەم کۆدە نیشانی کڕیارەکانت بدە یان چاپی بکە</p>
<div id="qr-code-container" class="flex justify-center mb-6 bg-white p-4 rounded-xl border border-slate-100 shadow-sm mx-auto w-max"></div>
<button onclick="window.print()" class="w-full bg-slate-800 hover:bg-slate-900 text-white font-bold py-3 rounded-xl shadow-md transition-colors flex justify-center items-center gap-2">
<i class="fa-solid fa-print"></i> چاپکردن / طباعة
</button>
</div>
</div>
<!-- Toast Notification -->
<div id="toast" class="fixed bottom-6 left-1/2 transform -translate-x-1/2 bg-slate-800/95 backdrop-blur-sm text-white px-6 py-3.5 rounded-2xl shadow-2xl z-[90] hidden opacity-0 transition-all flex items-center gap-3 w-max max-w-[90%] font-medium border border-slate-700">
<i class="fa-solid fa-circle-check text-green-400 text-lg" id="toast-icon"></i>
<span id="toast-msg" class="text-sm">سەرکەوتووبوو</span>
</div>
<!-- Confirm Modal -->
<div id="confirm-modal" class="fixed inset-0 z-[95] hidden flex items-center justify-center">
<div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm fade-in" onclick="closeConfirmModal()"></div>
<div class="bg-white p-6 rounded-3xl shadow-2xl z-10 w-[90%] max-w-sm text-center modal-enter relative">
<div class="w-16 h-16 bg-red-100 text-red-500 rounded-full flex items-center justify-center mx-auto mb-4">
<i class="fa-solid fa-triangle-exclamation text-3xl"></i>
</div>
<h3 class="font-bold text-xl text-slate-800 mb-2" id="confirm-title">تۆ یێ پشتڕاستی؟</h3>
<p class="text-sm text-slate-500 mb-6" id="confirm-desc">دڤێی ڤێ خوارنێ ژێببەی؟</p>
<div class="flex gap-3">
<button id="btn-confirm-yes" class="flex-1 bg-red-500 hover:bg-red-600 text-white font-bold py-3 rounded-xl shadow-md transition-colors">بەڵێ</button>
<button id="btn-confirm-no" class="flex-1 bg-slate-200 hover:bg-slate-300 text-slate-700 font-bold py-3 rounded-xl transition-colors">نەخێر</button>
</div>
</div>
</div>
<!-- Size Selection Modal -->
<div id="size-modal" class="fixed inset-0 z-[80] hidden flex items-center justify-center">
<div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm fade-in" onclick="closeSizeModal()"></div>
<div class="bg-white p-5 rounded-3xl shadow-2xl z-10 w-[90%] max-w-sm relative modal-enter">
<button onclick="closeSizeModal()" class="absolute top-4 left-4 w-8 h-8 rounded-full bg-slate-100 text-slate-500 flex items-center justify-center hover:bg-slate-200 transition-colors">
<i class="fa-solid fa-times"></i>
</button>
<h3 class="font-black text-xl text-slate-800 mb-5 mt-2 text-center" id="ui-size-title">قەبارە هەڵبژێرە</h3>
<div class="flex flex-col gap-3" id="size-options-container"></div>
</div>
</div>
<script type="module">
import { initializeApp } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-app.js";
import { getAuth, signInAnonymously, signInWithCustomToken } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-auth.js";
import { getFirestore, doc, setDoc, deleteDoc, onSnapshot, collection } from "https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore.js";
const appId = typeof __app_id !== 'undefined' ? __app_id : 'pizza-isfka-app';
const firebaseConfig = typeof __firebase_config !== 'undefined' ? JSON.parse(__firebase_config) : {};
let app = initializeApp(firebaseConfig);
let auth = getAuth(app);
let db = getFirestore(app);
window.toggleLanguage = toggleLanguage;
window.toggleCart = toggleCart;
window.checkout = checkout;
window.clearCart = clearCart;
window.promptAdmin = promptAdmin;
window.closeAuthModal = closeAuthModal;
window.handleAuth = handleAuth;
window.logoutAdmin = logoutAdmin;
window.closeAdminPanel = closeAdminPanel;
window.openEditForm = openEditForm;
window.closeEditForm = closeEditForm;
window.handleImageUpload = handleImageUpload;
window.removeUploadedImage = removeUploadedImage;
window.attemptDelete = attemptDelete;
window.attemptReset = attemptReset;
window.showQRCode = showQRCode;
window.closeQRCode = closeQRCode;
window.addToCart = addToCart;
window.updateCartQuantity = updateCartQuantity;
window.closeConfirmModal = closeConfirmModal;
window.closeSizeModal = closeSizeModal;
window.processAddToCart = processAddToCart;
const defaultData = {
categories: [
{ id: 'all', name_ku: 'هەمی', name_ar: 'الكل', icon: 'fa-border-all' },
{ id: 'favorites', name_ku: 'بژارتی', name_ar: 'المميزة', icon: 'fa-star' },
{ id: 'pizza', name_ku: 'پیزا', name_ar: 'بيتزا', icon: 'fa-pizza-slice' },
{ id: 'fastfood', name_ku: 'خوارنێن ب لەز', name_ar: 'وجبات سريعة', icon: 'fa-burger' },
{ id: 'drinks', name_ku: 'ڤەخوارن', name_ar: 'مشروبات', icon: 'fa-martini-glass-citrus' }
],
items: [
{
id: 'item_1',
categoryId: 'pizza',
name_ku: 'پیزا مارگریتا',
name_ar: 'بيتزا مارغريتا',
desc_ku: 'پەنیرێ مۆزەرێلا، سۆسا تەماتێ، زەیتون و قارچک.',
desc_ar: 'جبنة موزاريلا، صلصة طماطم، زيتون وفطر.',
price: 8000,
image: 'https://images.unsplash.com/photo-1574071318508-1cdbab80d002?q=80&w=600&auto=format&fit=crop'
},
{
id: 'item_2',
categoryId: 'pizza',
name_ku: 'پیزا پاپارۆنی',
name_ar: 'بيتزا بيبيروني',
desc_ku: 'پەنیرێ فرێش، پاپارۆنی، و سۆسا تایبەت.',
desc_ar: 'جبنة طازجة، بيبيروني، وصلصة خاصة.',
price: 10000,
image: 'https://images.unsplash.com/photo-1628840042765-356cda07504e?q=80&w=600&auto=format&fit=crop'
},
{
id: 'item_3',
categoryId: 'fastfood',
name_ku: 'بەرگەرێ گۆشتی',
name_ar: 'برغر لحم',
desc_ku: 'گۆشتێ فرێش، پەنیر، و کەسکاتی.',
desc_ar: 'لحم طازج، جبنة، وخضروات.',
price: 6500,
image: 'https://images.unsplash.com/photo-1568901346375-23c9450c58cd?q=80&w=600&auto=format&fit=crop'
},
{
id: 'item_4',
categoryId: 'drinks',
name_ku: 'ئاڤا پرتەقالێ فرێش',
name_ar: 'عصير برتقال طازج',
desc_ku: 'ئاڤا پرتەقالێ یا سروشتی بێ شەکر.',
desc_ar: 'عصير طبيعي بدون سكر.',
price: 3000,
image: 'https://images.unsplash.com/photo-1613478223719-2ab802602423?q=80&w=600&auto=format&fit=crop'
}
]
};
const i18n = {
ku: {
brand: "Isifka Pizza", heroTitle: "تامەکا ژبیرنەکری", heroSubtitle: "تامکرنا باشترین خوارنان",
langToggle: "ع", cartTitle: "سەلکا من", emptyCart: "سەلک ڤالایە", total: "کۆژمێ گشتی:",
checkout: "هنارتنا داخازیێ", add: "زێدەکرن", addedMsg: "ل سەلکێ هاتە زێدەکرن",
clearCart: "پاشگەزبوونەوە", clearCartMsg: "سەلک هاتە ڤالاکرن",
adminTitle: "ڕێڤەبەری", adminItemsTitle: "لیستا خوارنان", addNew: "زێدەکرن",
edit: "گوهۆرین", confirmTitle: "تۆ یێ پشتڕاستی؟", confirmDelete: "دڤێی ڤێ خوارنێ ژێببەی؟",
confirmReset: "دڤێی داتایان بگەڕێنی بۆ سەرەتایی؟ (ئەمە داتای سێرڤەر دەسڕێتەوە)",
saveSuccess: "ب سەرکەفتیانە هاتە پاراستن!", authTitle: "چوونەژوورەوەی بەڕێوەبەر",
authDesc: "تکایە تێپەڕەوشە بنووسە", loginBtn: "چوونەژوورەوە",
wrongPass: "تێپەڕەوشە هەڵەیە!", yes: "بەڵێ", no: "نەخێر", save: "پاراستن", cancel: "هەلوەشاندن", lock: "قوفڵکردن",
notesLabel: "تێبینی:", notesPlaceholder: "بۆ نموونە: بێ پیاز...",
selectSize: "قەبارەی پیزا هەڵبژێرە",
tableLabel: "ژمارەی مێز:", tablePlaceholder: "0"
},
ar: {
brand: "Isifka Pizza", heroTitle: "مذاق لا يُنسى", heroSubtitle: "تذوق أفضل الأطباق",
langToggle: "کوردی", cartTitle: "سلتي", emptyCart: "السلة فارغة", total: "المجموع الكلي:",
checkout: "إرسال الطلب", add: "إضافة", addedMsg: "تمت الإضافة للسلة",
clearCart: "تفريغ السلة", clearCartMsg: "تم تفريغ السلة",
adminTitle: "الإدارة", adminItemsTitle: "قائمة الأطباق", addNew: "إضافة جديد",
edit: "تعديل", confirmTitle: "هل أنت متأكد؟", confirmDelete: "هل تريد حذف هذا الطبق؟",
confirmReset: "هل تريد إعادة ضبط البيانات بالكامل؟ (سيتم مسح بيانات الخادم)",
saveSuccess: "تم الحفظ بنجاح!", authTitle: "تسجيل دخول الإدارة",
authDesc: "الرجاء إدخال كلمة المرور", loginBtn: "دخول",
wrongPass: "كلمة المرور خاطئة!", yes: "نعم", no: "كلا", save: "حفظ", cancel: "إلغاء", lock: "قفل",
notesLabel: "ملاحظات:", notesPlaceholder: "مثال: بدون بصل...",
selectSize: "اختر حجم البيتزا",
tableLabel: "رقم الطاولة:", tablePlaceholder: "0"
}
};
let lang = 'ku';
let appData = { categories: defaultData.categories, items: [] };
let activeCategory = 'all';
let cart = [];
let isAdminAuthed = false;
const ADMIN_PASSWORD = "12345";
let currentUploadedImageBase64 = '';
async function initApp() {
try {
const initialToken = typeof __initial_auth_token !== 'undefined' ? __initial_auth_token : null;
if (initialToken) {
await signInWithCustomToken(auth, initialToken);
} else {
await signInAnonymously(auth);
}
document.getElementById('sync-status').classList.remove('hidden');
setupFirestoreListeners();
applyTranslations();
} catch (error) {
console.error("Firebase Auth Error:", error);
document.getElementById('loading-indicator').innerHTML = '<p class="text-red-500 font-bold">کێشە لە پەیوەندیکردن بە سێرڤەر هەیە!</p>';
}
}
function setupFirestoreListeners() {
const itemsRef = collection(db, 'artifacts', appId, 'public', 'data', 'menuItems');
onSnapshot(itemsRef, (snapshot) => {
const newItems = [];
snapshot.forEach((doc) => {
newItems.push({ id: doc.id, ...doc.data() });
});
appData.items = newItems;
if (newItems.length === 0) {
defaultData.items.forEach(async (item) => {
const { id, ...data } = item;
await setDoc(doc(db, 'artifacts', appId, 'public', 'data', 'menuItems', id), data);
});
}
document.getElementById('loading-indicator').style.display = 'none';
document.getElementById('menu-items-container').classList.remove('hidden');
renderAdminItems();
renderCategories();
renderItems();
updateCartUI();
}, (error) => {
console.error("Firestore listen error:", error);
showToast("کێشە لە نوێکردنەوەی زانیارییەکان هەیە", "error");
});
}
function setElText(id, text) { const el = document.getElementById(id); if (el && text) el.innerText = text; }
function setElHtml(id, html) { const el = document.getElementById(id); if (el && html) el.innerHTML = html; }
function applyTranslations() {
const t = i18n[lang];
document.documentElement.lang = lang;
document.documentElement.dir = 'rtl';
setElText('ui-brand-title', t.brand); setElText('ui-hero-title', t.heroTitle);
setElText('ui-hero-subtitle', t.heroSubtitle); setElText('ui-btn-lang', t.langToggle);
setElHtml('ui-cart-title', ⁠<i class="fa-solid fa-basket-shopping text-rose-500"></i> ${t.cartTitle}⁠);
setElText('ui-empty-cart', t.emptyCart); setElText('ui-total-text', t.total);
setElHtml('ui-btn-checkout', ⁠${t.checkout} <i class="fa-brands fa-whatsapp text-2xl"></i>⁠);
const clearBtnSpan = document.querySelector('#ui-btn-clear-cart span');
if(clearBtnSpan) clearBtnSpan.innerText = t.clearCart;
setElText('ui-notes-label', t.notesLabel);
const notesEl = document.getElementById('order-notes');
if(notesEl) notesEl.placeholder = t.notesPlaceholder;
setElText('ui-table-label', t.tableLabel);
const tableEl = document.getElementById('table-number');
if(tableEl) tableEl.placeholder = t.tablePlaceholder;
setElText('ui-admin-title', t.adminTitle); setElHtml('ui-admin-items-title', ⁠<i class="fa-solid fa-list-ul ml-2 text-blue-400"></i> ${t.adminItemsTitle}⁠);
setElText('ui-btn-add-new', t.addNew); setElText('ui-auth-title', t.authTitle);
setElText('ui-auth-desc', t.authDesc); setElText('ui-btn-login', t.loginBtn);
setElText('ui-btn-save', t.save); setElText('ui-btn-cancel', t.cancel);
setElText('ui-btn-lock', t.lock);
renderCategories(); renderItems(); updateCartUI();
}
function toggleLanguage() {
lang = lang === 'ku' ? 'ar' : 'ku';
applyTranslations();
}
function renderCategories() {
const container = document.getElementById('categories-container');
if(!container) return;
container.innerHTML = '';
appData.categories.forEach(cat => {
const btn = document.createElement('button');
const catName = lang === 'ku' ? cat.name_ku : cat.name_ar;
const icon = cat.icon || 'fa-utensils';
btn.className = ⁠cat-btn whitespace-nowrap px-6 py-2.5 rounded-full text-sm font-bold flex items-center gap-2 ${activeCategory === cat.id ? 'active' : ''}⁠;
btn.innerHTML = ⁠<i class="fa-solid ${icon} opacity-80"></i> ${catName}⁠;
btn.onclick = () => { activeCategory = cat.id; renderCategories(); renderItems(); };
container.appendChild(btn);
});
}
function formatPrice(price) { return new Intl.NumberFormat('en-US').format(price); }
function renderItems() {
const container = document.getElementById('menu-items-container');
if(!container) return;
container.innerHTML = '';
const t = i18n[lang];
const filteredItems = activeCategory === 'all'
? appData.items
: appData.items.filter(item => item.categoryId === activeCategory);
if(filteredItems.length === 0) {
container.innerHTML = ⁠<div class="col-span-1 sm:col-span-2 text-center text-slate-500 py-10 font-bold">هیچ خوارنەک نەهاتە دیتن / لا توجد أطباق</div>⁠;
return;
}
filteredItems.forEach(item => {
const name = lang === 'ku' ? item.name_ku : item.name_ar;
const desc = lang === 'ku' ? item.desc_ku : item.desc_ar;
const img = item.image || 'https://placehold.co/400x300/fecaca/ef4444?text=Food';
const card = document.createElement('div');
card.className = 'food-card bg-white rounded-[24px] shadow-sm border border-slate-100/60 overflow-hidden flex flex-row h-40';
card.innerHTML = ⁠<div class="w-2/5 relative"> <img src="${img}" alt="${name}" class="w-full h-full object-cover"> <div class="absolute inset-0 bg-gradient-to-r from-transparent to-white opacity-20"></div> </div> <div class="p-4 w-3/5 flex flex-col justify-between bg-white relative"> <div> <h3 class="font-black text-slate-800 text-lg leading-tight line-clamp-1 mb-1">${name}</h3> <p class="text-xs text-slate-500 line-clamp-2 leading-relaxed font-medium">${desc}</p> </div> <div class="flex justify-between items-end mt-2 pt-2 border-t border-slate-50 border-dashed"> <div class="flex flex-col" dir="ltr"> <span class="font-black text-rose-600 text-lg leading-none">${formatPrice(item.price)}</span> <span class="text-[10px] text-slate-400 font-bold tracking-widest text-right">IQD</span> </div> <button onclick="addToCart('${item.id}')" class="bg-gradient-to-tr from-rose-500 to-orange-400 text-white w-9 h-9 rounded-[12px] flex items-center justify-center shadow-md shadow-rose-200 active:scale-90 transition-all group" title="${t.add}"> <i class="fa-solid fa-plus group-hover:rotate-90 transition-transform duration-300"></i> </button> </div> </div>⁠;
container.appendChild(card);
});
}
function addToCart(itemId) {
const item = appData.items.find(i => i.id === itemId);
if(!item) return;
if(item.categoryId === 'pizza') {
openSizeModal(item);
} else {
processAddToCart(item, 'regular', item.price);
}
}
function openSizeModal(item) {
const t = i18n[lang];
document.getElementById('ui-size-title').innerText = t.selectSize;
const container = document.getElementById('size-options-container');
container.innerHTML = '';
const pMedium = item.price;
const pSmall = Math.max(1000, pMedium - 2000);
const pLarge = pMedium + 3000;
const sizes = [
{ id: 'small', name_ku: 'بچووک', name_ar: 'صغير', price: pSmall, icon: 'fa-pizza-slice text-sm' },
{ id: 'medium', name_ku: 'ناوەند', name_ar: 'وسط', price: pMedium, icon: 'fa-pizza-slice text-lg' },
{ id: 'large', name_ku: 'مەزن', name_ar: 'كبير', price: pLarge, icon: 'fa-pizza-slice text-xl' }
];
sizes.forEach(size => {
const sizeName = lang === 'ku' ? size.name_ku : size.name_ar;
const btn = document.createElement('button');
btn.className = 'w-full flex justify-between items-center p-4 bg-slate-50 hover:bg-orange-50 border border-slate-200 hover:border-orange-300 rounded-2xl transition-all group active:scale-95';
btn.onclick = () => {
processAddToCart(item, size.id, size.price, sizeName);
closeSizeModal();
};
btn.innerHTML = ⁠<div class="flex items-center gap-3"> <div class="w-10 h-10 rounded-full bg-white flex items-center justify-center shadow-sm group-hover:text-orange-500 text-slate-400 transition-colors"> <i class="fa-solid ${size.icon}"></i> </div> <span class="font-bold text-slate-700 group-hover:text-orange-600 transition-colors">${sizeName}</span> </div> <div class="font-black text-slate-800" dir="ltr">${formatPrice(size.price)} <span class="text-[10px] text-slate-400">IQD</span></div>⁠;
container.appendChild(btn);
});
document.getElementById('size-modal').classList.remove('hidden');
}
function closeSizeModal() {
document.getElementById('size-modal').classList.add('hidden');
}
function processAddToCart(baseItem, sizeId, finalPrice, sizeName = '') {
const uniqueId = sizeId === 'regular' ? baseItem.id : ⁠${baseItem.id}_${sizeId}⁠;
const existingItem = cart.find(i => i.uniqueId === uniqueId);
if (existingItem) {
existingItem.quantity += 1;
} else {
cart.push({
...baseItem,
uniqueId: uniqueId,
cartPrice: finalPrice,
sizeName_ku: sizeId === 'regular' ? '' : ⁠ (${sizeId === 'small' ? 'بچووک' : sizeId === 'medium' ? 'ناوەند' : 'مەزن'})⁠,
sizeName_ar: sizeId === 'regular' ? '' : ⁠ (${sizeId === 'small' ? 'صغير' : sizeId === 'medium' ? 'وسط' : 'كبير'})⁠,
quantity: 1
});
}
updateCartUI();
showToast(i18n[lang].addedMsg, 'success');
}
function updateCartQuantity(uniqueId, change) {
const index = cart.findIndex(i => i.uniqueId === uniqueId);
if (index > -1) {
cart[index].quantity += change;
if (cart[index].quantity <= 0) cart.splice(index, 1);
}
updateCartUI();
}
function clearCart() {
if (cart.length === 0) return;
cart = [];
const notesEl = document.getElementById('order-notes');
if(notesEl) notesEl.value = '';
const tableEl = document.getElementById('table-number');
if(tableEl) tableEl.value = '';
updateCartUI();
showToast(i18n[lang].clearCartMsg, 'success');
}
function updateCartUI() {
const badge = document.getElementById('cart-badge');
const clearBtn = document.getElementById('ui-btn-clear-cart');
const totalQuantity = cart.reduce((sum, item) => sum + item.quantity, 0);
if (totalQuantity > 0) {
badge.innerText = totalQuantity; badge.classList.remove('hidden');
if(clearBtn) clearBtn.classList.remove('hidden');
} else {
badge.classList.add('hidden');
if(clearBtn) clearBtn.classList.add('hidden');
}
const container = document.getElementById('cart-items-container');
const emptyMsg = document.getElementById('empty-cart-msg');
Array.from(container.children).forEach(child => { if(child.id !== 'empty-cart-msg') child.remove(); });
if (cart.length === 0) {
emptyMsg.style.display = 'flex';
document.getElementById('cart-total').innerHTML = ⁠0 IQD⁠;
} else {
emptyMsg.style.display = 'none';
let totalPrice = 0;
cart.forEach(item => {
const latestItemData = appData.items.find(i => i.id === item.id) || item;
const baseName = lang === 'ku' ? latestItemData.name_ku : latestItemData.name_ar;
const sizeSuffix = lang === 'ku' ? (item.sizeName_ku || '') : (item.sizeName_ar || '');
const finalName = baseName + sizeSuffix;
const finalPrice = item.cartPrice || latestItemData.price;
totalPrice += (finalPrice * item.quantity);
const el = document.createElement('div');
el.className = 'flex justify-between items-center p-3.5 bg-white rounded-[20px] shadow-sm border border-slate-100';
el.innerHTML = ⁠<div class="flex-1 flex gap-3 items-center"> <img src="${latestItemData.image}" class="w-12 h-12 rounded-xl object-cover shadow-sm border border-slate-50"> <div> <h4 class="font-bold text-sm text-slate-800 line-clamp-1">${finalName}</h4> <div class="text-rose-600 text-sm font-black mt-0.5" dir="ltr">${formatPrice(finalPrice)} <span class="text-[10px] font-normal text-slate-400">IQD</span></div> </div> </div> <div class="flex items-center gap-3 bg-slate-50 rounded-[14px] p-1.5 border border-slate-200"> <button onclick="updateCartQuantity('${item.uniqueId}', 1)" class="w-7 h-7 flex items-center justify-center rounded-[10px] bg-white text-slate-700 shadow-sm hover:bg-rose-50 hover:text-rose-600 transition-colors"><i class="fa-solid fa-plus text-[11px]"></i></button> <span class="font-bold text-sm w-4 text-center">${item.quantity}</span> <button onclick="updateCartQuantity('${item.uniqueId}', -1)" class="w-7 h-7 flex items-center justify-center rounded-[10px] bg-white text-slate-700 shadow-sm hover:bg-rose-50 hover:text-rose-600 transition-colors"><i class="fa-solid fa-minus text-[11px]"></i></button> </div>⁠;
container.appendChild(el);
});
document.getElementById('cart-total').innerHTML = ⁠${formatPrice(totalPrice)} IQD⁠;
}
}
function toggleCart() {
const modal = document.getElementById('cart-modal');
const content = document.getElementById('cart-content');
if (modal.classList.contains('hidden')) {
modal.classList.remove('hidden');
setTimeout(() => content.classList.remove('translate-y-full'), 10);
document.body.style.overflow = 'hidden';
} else {
content.classList.add('translate-y-full');
setTimeout(() => modal.classList.add('hidden'), 300);
document.body.style.overflow = 'auto';
}
}
function checkout() {
if (cart.length === 0) return;
const phone = "9647517881990";
let msg = lang === 'ku' ? "سلاڤ، من ئەڤ داخازیە هەیە:\n\n" : "مرحباً، لدي هذا الطلب:\n\n";
let total = 0;
const tableEl = document.getElementById('table-number');
const tableNum = tableEl ? tableEl.value.trim() : "";
if (tableNum) {
const tableTitle = lang === 'ku' ? "ژمارەی مێز:" : "رقم الطاولة:";
msg += ⁠🪑 ${tableTitle} *${tableNum}*\n\n⁠;
}
cart.forEach(item => {
const latestItemData = appData.items.find(i => i.id === item.id) || item;
const baseName = lang === 'ku' ? latestItemData.name_ku : latestItemData.name_ar;
const sizeSuffix = lang === 'ku' ? (item.sizeName_ku || '') : (item.sizeName_ar || '');
const finalName = baseName + sizeSuffix;
const finalPrice = item.cartPrice || latestItemData.price;
msg += ⁠▪ ${finalName} - ${item.quantity}x\n⁠;
total += (finalPrice * item.quantity);
});
const notesEl = document.getElementById('order-notes');
const notes = notesEl ? notesEl.value.trim() : "";
if (notes) {
const notesTitle = lang === 'ku' ? "تێبینی:" : "ملاحظات:";
msg += ⁠\n📝 ${notesTitle}\n${notes}\n⁠;
}
const totalText = lang === 'ku' ? "کۆژمێ گشتی" : "المجموع الكلي";
msg += ⁠\n=================\n${totalText}: *${formatPrice(total)} IQD*⁠;
const encodedMsg = encodeURIComponent(msg);
const url = ⁠https://wa.me/${phone}?text=${encodedMsg}⁠;
const link = document.createElement('a');
link.href = url;
link.target = '_blank';
document.body.appendChild(link);
link.click();
document.body.removeChild(link);
}
function promptAdmin() {
if (isAdminAuthed) openAdminPanel();
else {
document.getElementById('auth-modal').classList.remove('hidden');
setTimeout(() => document.getElementById('admin-pass-input').focus(), 100);
}
}
function closeAuthModal() {
document.getElementById('auth-modal').classList.add('hidden');
document.getElementById('admin-pass-input').value = '';
}
function handleAuth(e) {
e.preventDefault();
const pass = document.getElementById('admin-pass-input').value;
if (pass === ADMIN_PASSWORD) {
isAdminAuthed = true; closeAuthModal(); openAdminPanel();
} else {
showToast(i18n[lang].wrongPass, 'error');
document.getElementById('admin-pass-input').value = '';
}
}
function logoutAdmin() {
isAdminAuthed = false; closeAdminPanel();
showToast(lang === 'ku' ? "دەرچوون سەرکەوتووبوو" : "تم تسجيل الخروج", 'success');
}
function openAdminPanel() {
document.getElementById('admin-modal').classList.remove('hidden');
document.body.style.overflow = 'hidden';
renderAdminItems(); populateAdminCategories();
}
function closeAdminPanel() {
document.getElementById('admin-modal').classList.add('hidden');
document.body.style.overflow = 'auto'; closeEditForm();
}
function renderAdminItems() {
const container = document.getElementById('admin-items-list');
container.innerHTML = '';
if(appData.items.length === 0) {
container.innerHTML = ⁠<div class="col-span-1 sm:col-span-2 text-center text-slate-500 py-4 font-bold">داتابەیس ڤالایە، خوارنان زێدە بکە</div>⁠;
}
appData.items.forEach(item => {
const el = document.createElement('div');
el.className = 'flex justify-between items-center p-3 bg-white rounded-2xl shadow-sm border border-slate-100';
el.innerHTML = ⁠<div class="flex items-center gap-3"> <img src="${item.image}" class="w-14 h-14 rounded-xl object-cover border border-slate-100"> <div> <p class="font-bold text-sm text-slate-800 line-clamp-1">${item.name_ku}</p> <p class="font-black text-blue-600 text-xs mt-1" dir="ltr">${formatPrice(item.price)} <span class="text-[9px] text-slate-400">IQD</span></p> </div> </div> <div class="flex gap-2 shrink-0"> <button onclick="openEditForm('${item.id}')" class="w-9 h-9 rounded-xl bg-blue-50 text-blue-600 hover:bg-blue-600 hover:text-white transition-colors flex items-center justify-center"><i class="fa-solid fa-pen-to-square"></i></button> <button onclick="attemptDelete('${item.id}')" class="w-9 h-9 rounded-xl bg-red-50 text-red-600 hover:bg-red-600 hover:text-white transition-colors flex items-center justify-center"><i class="fa-solid fa-trash-can"></i></button> </div>⁠;
container.appendChild(el);
});
}
function populateAdminCategories() {
const select = document.getElementById('form-category');
select.innerHTML = '';
appData.categories.forEach(cat => {
if(cat.id === 'all') return;
const option = document.createElement('option');
option.value = cat.id; option.innerText = ⁠${cat.name_ku} / ${cat.name_ar}⁠;
select.appendChild(option);
});
}
function handleImageUpload(event) {
const file = event.target.files[0];
if (file) {
const reader = new FileReader();
reader.onload = function(e) {
const img = new Image();
img.onload = function() {
const canvas = document.createElement('canvas');
const MAX_SIZE = 500; let width = img.width, height = img.height;
if (width > height) { if (width > MAX_SIZE) { height *= MAX_SIZE / width; width = MAX_SIZE; } }
else { if (height > MAX_SIZE) { width *= MAX_SIZE / height; height = MAX_SIZE; } }
canvas.width = width; canvas.height = height;
const ctx = canvas.getContext('2d'); ctx.drawImage(img, 0, 0, width, height);
currentUploadedImageBase64 = canvas.toDataURL('image/jpeg', 0.6);
document.getElementById('image-preview').src = currentUploadedImageBase64;
document.getElementById('image-preview-container').classList.remove('hidden');
};
img.src = e.target.result;
};
reader.readAsDataURL(file);
}
}
function removeUploadedImage() {
currentUploadedImageBase64 = '';
document.getElementById('form-image-file').value = '';
document.getElementById('image-preview-container').classList.add('hidden');
document.getElementById('image-preview').src = '';
}
function openEditForm(itemId = null) {
const formContainer = document.getElementById('admin-form-container');
formContainer.classList.remove('hidden'); removeUploadedImage();
if (itemId) {
const item = appData.items.find(i => i.id === itemId);
document.getElementById('ui-form-title').innerHTML = ⁠<i class="fa-solid fa-pen-to-square text-blue-500"></i> ${i18n[lang].edit}⁠;
document.getElementById('form-id').value = item.id; document.getElementById('form-category').value = item.categoryId;
document.getElementById('form-name-ku').value = item.name_ku; document.getElementById('form-name-ar').value = item.name_ar;
document.getElementById('form-desc-ku').value = item.desc_ku; document.getElementById('form-desc-ar').value = item.desc_ar;
document.getElementById('form-price').value = item.price; currentUploadedImageBase64 = item.image;
} else {
document.getElementById('ui-form-title').innerHTML = ⁠<i class="fa-solid fa-plus text-blue-500"></i> ${i18n[lang].addNew}⁠;
document.getElementById('admin-form').reset(); document.getElementById('form-id').value = '';
}
formContainer.scrollIntoView({ behavior: 'smooth' });
}
function closeEditForm() {
document.getElementById('admin-form-container').classList.add('hidden');
document.getElementById('admin-form').reset(); removeUploadedImage();
}
document.getElementById('admin-form').addEventListener('submit', async function(e) {
e.preventDefault();
const btn = document.getElementById('ui-btn-save');
btn.innerHTML = '<i class="fa-solid fa-circle-notch fa-spin"></i>';
btn.disabled = true;
const id = document.getElementById('form-id').value || 'item_' + Date.now();
const finalImage = currentUploadedImageBase64 || 'https://placehold.co/400x300/fecaca/ef4444?text=Food';
const newItem = {
categoryId: document.getElementById('form-category').value,
name_ku: document.getElementById('form-name-ku').value,
name_ar: document.getElementById('form-name-ar').value,
desc_ku: document.getElementById('form-desc-ku').value,
desc_ar: document.getElementById('form-desc-ar').value,
price: parseInt(document.getElementById('form-price').value),
image: finalImage
};
try {
await setDoc(doc(db, 'artifacts', appId, 'public', 'data', 'menuItems', id), newItem);
closeEditForm();
showToast(i18n[lang].saveSuccess, 'success');
} catch (err) {
console.error("Error saving:", err);
showToast("کێشەیەک هەیە لە پەیوەندی هێڵەکەدا", 'error');
} finally {
btn.innerHTML = i18n[lang].save;
btn.disabled = false;
}
});
function attemptDelete(itemId) {
showConfirmModal(i18n[lang].confirmTitle, i18n[lang].confirmDelete, async () => {
try {
await deleteDoc(doc(db, 'artifacts', appId, 'public', 'data', 'menuItems', itemId));
cart = cart.filter(i => i.id !== itemId);
showToast(i18n[lang].saveSuccess, 'success');
} catch(err) {
showToast("کێشەیەک هەیە لە سڕینەوە", "error");
}
});
}
function attemptReset() {
showConfirmModal(i18n[lang].confirmTitle, i18n[lang].confirmReset, async () => {
try {
for(let i of appData.items) {
await deleteDoc(doc(db, 'artifacts', appId, 'public', 'data', 'menuItems', i.id));
}
for(let item of defaultData.items) {
const { id, ...data } = item;
await setDoc(doc(db, 'artifacts', appId, 'public', 'data', 'menuItems', id), data);
}
closeEditForm();
showToast("بە سەرکەوتوویی گەڕێنرایەوە", 'success');
} catch(err) {
showToast("کێشەیەک ڕوویدا لە کاتی گەڕاندنەوە", "error");
}
});
}
function showConfirmModal(title, desc, onConfirm) {
const t = i18n[lang];
document.getElementById('confirm-title').innerText = title;
document.getElementById('confirm-desc').innerText = desc;
document.getElementById('btn-confirm-yes').innerText = t.yes;
document.getElementById('btn-confirm-no').innerText = t.no;
const newYesBtn = document.getElementById('btn-confirm-yes').cloneNode(true);
document.getElementById('btn-confirm-yes').replaceWith(newYesBtn);
newYesBtn.addEventListener('click', () => {
if(onConfirm) onConfirm();
closeConfirmModal();
});
document.getElementById('confirm-modal').classList.remove('hidden');
}
function closeConfirmModal() { document.getElementById('confirm-modal').classList.add('hidden'); }
document.getElementById('btn-confirm-no').addEventListener('click', closeConfirmModal);
function showQRCode() {
document.getElementById('qr-modal').classList.remove('hidden');
const container = document.getElementById('qr-code-container');
container.innerHTML = '';
const currentUrl = window.location.href.split('#')[0];
new QRCode(container, {
text: currentUrl,
width: 180,
height: 180,
colorDark : "#0f172a",
colorLight : "#ffffff",
correctLevel : QRCode.CorrectLevel.H
});
}
function closeQRCode() { document.getElementById('qr-modal').classList.add('hidden'); }
let toastTimeout;
function showToast(message, type = 'success') {
const toast = document.getElementById('toast');
const icon = document.getElementById('toast-icon');
document.getElementById('toast-msg').innerText = message;
icon.className = type === 'error' ? 'fa-solid fa-circle-xmark text-red-500 text-lg' : 'fa-solid fa-circle-check text-green-400 text-lg';
toast.classList.remove('hidden'); toast.classList.add('modal-enter'); toast.style.opacity = '1';
clearTimeout(toastTimeout);
toastTimeout = setTimeout(() => {
toast.style.opacity = '0';
setTimeout(() => toast.classList.add('hidden'), 300);
}, 3000);
}
initApp();
</script>
</body>
</html>